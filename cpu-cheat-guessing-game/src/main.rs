/*
WORK IN PROGRESS
Need binary tree to get the longest path for finding a number
*/

use std::io;
use std::process;

const MAX_SECRET: u8 = 10;
fn main() {
    /* TODO write a cheating guessing game
    println!("Welcome to cheating guessing game.");
    println!("Please choose a number between 0 and {}", MAX_SECRET);
    let mut guess = String::new();

    if let Err(e) = io::stdin().read_line(&mut guess) {
        eprintln!("Error: {}", e);
        process::exit(1);
    }
    */
}
